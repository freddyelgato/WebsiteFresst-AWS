# auto-generated by ginshot
# Uncomment if needed
# version: '3.8'

services:
  orders-db:
    build:
      context: ./config/orders
      dockerfile: Dockerfile
    container_name: orders-db
    environment:
      MONGO_INITDB_ROOT_USERNAME: user
      MONGO_INITDB_ROOT_PASSWORD: password
    ports:
      - "27017:27017"
    networks:
      - app-network

  # edit-order API Container
  edit-order:
    build: .
    ports:
      - "5001:5001"
    container_name: edit-order
    environment:
      - GIN_MODE=release
      - orders_URI=mongodb://user:password@orders-db:27017
    volumes:
      - .:/app
    networks:
      - app-network
    restart: unless-stopped
    depends_on:
      - orders-db

networks:
  app-network:
    driver: bridge