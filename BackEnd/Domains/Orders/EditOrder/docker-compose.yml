# auto-generated by ginshot
# Uncomment if needed
# version: '3.8'

services:
  # edit-order API Container
  edit-order:
    build: .
    ports:
      - "5001:5001"
    container_name: edit-order
    environment:
      - GIN_MODE=release
    volumes:
      - .:/app
    networks:
      - app-network
    restart: unless-stopped

networks:
  app-network:
    driver: bridge