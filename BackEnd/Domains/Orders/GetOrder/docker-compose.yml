# auto-generated by ginshot
# Uncomment if needed
# version: '3.8'

services:
  orders-db:
    build:
      context: ./config/orders
      dockerfile: Dockerfile
    container_name: orders-db-get
    environment:
      MONGO_INITDB_ROOT_USERNAME: user
      MONGO_INITDB_ROOT_PASSWORD: password
    ports:
      - "27021:27017"
    networks:
      - app-network

  # get-order API Container
  get-order:
    build: .
    ports:
      - "5002:5002"
    container_name: get-order
    environment:
      - GIN_MODE=release
      - orders_URI=mongodb://user:password@172.31.30.195:27017
    volumes:
      - .:/app
    networks:
      - app-network
    restart: unless-stopped
    depends_on:
      - orders-db

networks:
  app-network:
    driver: bridge